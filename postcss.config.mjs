/** @type {import('postcss-load-config').Config} */
const config = {
	plugins: {
		tailwindcss: {},
		autoprefixer: {},
		'postcss-import': {},
		'postcss-nested': {},
		'postcss-extend-rule': {},
	},
};

export default config;
